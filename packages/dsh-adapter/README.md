# Codekin DSH Adapter

This private workspace package translates DeepSeek Harness sessions, web routes, and persistence into calls against an explicitly supplied Codekin runtime. It does not own game rules or content definitions.
