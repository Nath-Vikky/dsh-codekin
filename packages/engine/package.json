{
  "name": "@nath-vikky/codekin-engine",
  "description": "Deterministic headless rules engine for Codekin",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/packages/engine/src/index.d.ts",
      "default": "./dist/packages/engine/src/index.js"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "node --eval \"import('node:fs/promises').then(({rm}) => rm('dist', { recursive: true, force: true }))\"",
    "typecheck": "tsc -p tsconfig.json --noEmit --pretty false"
  },
  "files": ["dist"],
  "license": "MIT"
}
