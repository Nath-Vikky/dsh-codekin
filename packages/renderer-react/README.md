# Codekin React Renderer

This private workspace package renders server-supplied, validated content views. It consumes engine state and actions but does not import the official core catalog or mechanics table.
